services:
  frontend:
    build:
      dockerfile: Dockerfile
      context: ./client
    ports:
      - "5173:80"
  backend:
    build:
      dockerfile: Dockerfile
      context: ./server
    ports:
      - "8000:8000"
